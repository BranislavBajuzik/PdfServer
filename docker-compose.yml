version: '3.7'

services:
  dataset-db:
    image: postgres:13
    environment:
      POSTGRES_PASSWORD: user1
      POSTGRES_USER: 12345
      POSTGRES_DB: pdf
    ports:
      - 5432:5432

volumes:
  pdf_storage:
